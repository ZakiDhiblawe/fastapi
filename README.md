# fastapi

run using uvicorn main:obj --reload

python3 -m venv venv

source venv/scripts/activate

pip install uvicorn fastapi


postgres driver 

sudo apt install libpq-dev python3-dev

pip install sqlalchemy psycopg2
